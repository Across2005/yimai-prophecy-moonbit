## 1. 逐条判定：真实严重度与理由（含代码证据）

| 编号 | 原严重度 | **复核后严重度** | 核心理由与确凿代码证据 |
|------|----------|------------------|-------------------------|
| **F1** | 中/正确性 | **⬆ 高/正确性** | `snapshot`（L770）只深拷贝 `memories`，未包含 `transitions`、`role_trans`；`consolidate`（L787-796）会衰减/剪枝转移边；`restore`（L858）仅恢复 `memories`。`consolidate(true)` 后 `restore` 导致转移矩阵不可逆丢失，回滚后 `predict` 行为与快照时刻不一致，破坏“约束契约回滚”核心语义。属**数据不变量破坏**，应定性为高危。 |
| **F2** | 中/数据持久化 | **⬆ 高/数据持久化** | `to_json`（L1004-1027）未序列化 `cur_episode` 与 `last_pred`；`from_json` 亦无恢复逻辑。往返测试（`accept_test` L163-177）因两字段本不在 JSON 中而“假通过”。运行中的回合信息与上一步预测上下文在重启后静默丢失，导致在线学习连续性中断。 |
| **F3** | 中/可解释性 | **⬆ 高/正确性** | `predict` L622-626：`uncertainty = -Array.fold (…) 0.0 top_k_probs`。L618-619 已对**全量 `dst`** 归一化，但熵仅对 `top_k` 计算。系统性低估不确定性，误导下游置信度判断，违反 D7 设计契约“整体分布熵”。 |
| **F4** | 中低/性能 | **⬇ 低/性能技术债** | `remember` 每次遍历全部记忆计算余弦（L296-302），`recall`、`consolidate` 均为线性扫描。目前 MoonBit 标准库无索引支持，在记忆规模 <10⁴ 时性能可接受，属架构债务但非当前瓶颈。应降级为低，标记为中期优化点。 |
| **F5** | 低 | **➡ 维持低** | `observe`（L657）`self.clock += 1` 后，`remember`（L292）又 `self.clock += 1`，每次 `observe` 时钟自增 2。相对顺序不变，但衰减速度等效于 `REC_TAU` 减半。不影响正确性，仅超参解读偏移，属低优先级清理项。 |
| **F6** | 低/可维护性 | **➡ 维持低** | `engine.mbt` 与 `cmd/main/main.mbt` 各自重复实现 JSON 取值辅助函数。功能无影响，仅代码整洁度问题。 |

**判断汇总**：F1、F2、F3 升级为高优先级正确性缺陷；F4 降级为低；F5、F6 维持原级。

---

## 2. 清单外隐患（盲区扫描）

| 隐患类型 | 描述 | 触发场景 / 代码定位 | 建议对策 |
|----------|------|---------------------|----------|
| **浮点数值稳定性** | `cosine_sim` 分母可能为 0 产生 `NaN`（L270-280）；`softmax` 未减 `max_logit` 导致 `exp` 溢出（L610-618）；`predict` 空分支 `uncertainty = 0.0` 而非最大不确定值（L600-630）。 | `engine.mbt` L270, L610, L600-630 | 除零时返回 0.0；softmax 使用 log-sum-exp 技巧；空分支返回 `1.0` 或 `Option<PredictResult>`。 |
| **CJK 分词边界** | `tokenize` 仅 `str.split(" ")`（L240），中日韩无空格语言退化为整句单 token，导致记忆图节点粒度过粗、语义丢失。 | `engine.mbt` L240 | 接入 `unicode-segmentation` 或提供 `tokenizer` 注入点，保留默认实现但文档说明。 |
| **JSON 反序列化健壮性** | `from_json` 直接下标+强转，缺键/类型不匹配时 panic（L1030-1060）。无版本字段与 schema 校验。 | `engine.mbt` L1030-1060 | 改为返回 `Result`，加入版本字段，并提供默认值或错误路径。 |
| **并发安全** | 所有字段 `mut`，无锁保护。MoonBit 当前单线程运行，但若未来接入 WASM 多线程或 FFI 并发，数据竞争必定出现。 | 全文件 `mut self` | 短期文档标注“非线程安全”；中期引入 `RwLock` 或 Actor 封装。 |
| **`consolidate` 硬剪枝副作用** | `hard=true` 时 `transitions.clear()`（L794-796）直接抹除转移，且无法凭当前快照恢复。若误调用则预测能力归零。 | L794-796 | 改为渐进式剪枝；提供 `dry_run` 预览；审计日志记录剪枝规模。 |
| **`predict` 空分支不确定性错误** | `transitions.get(src)` 为 `None` 时返回 `[]`，`uncertainty = 0.0`，语义应为“最大不确定”而非“完全确定”。 | L600-630 | 返回 `uncertainty = 1.0` 或 `Option` 让上层决策。 |
| **快照一致性缺口** | 除 F1 外，`snapshot` 还缺失 `context`、`meta_hits`、`clock` 等字段，`restore` 后上下文窗口、命中窗口、时钟均丢失。 | L770, L858 | 将快照扩展为包含全部可变字段的完整结构体。 |
| **测试覆盖盲区** | 仅覆盖单 episode 流程与 JSON 往返，缺少并发、异常 JSON、大规模性能、回滚一致性、分词边界、`consolidate` 软/硬对比等测试。 | `accept_test.mbt` | 增加 property-based 测试与混沌测试。 |

---

## 3. 按“快 / 准 / 美”排序的修复路线图

### 🔴 P0 ‒ “准”（正确性恢复）—— 本 Sprint 强制完成
| 任务 | 投入估算 | 收益 |
|------|----------|------|
| **完整快照 + 事务式 restore**（修复 F1 + 补全 `context`/`meta_hits`/`clock` 等） | ~3 人·天 | 消除核心不变量破坏，使回滚语义正确，为检查点与灾备奠基 |
| **JSON 版本化与健壮解析**（补全 `cur_episode`/`last_pred` 序列化、返回 `Result`、加入版本字段） | ~1.5 人·天 | 修复 F2 假阳性，支持可靠持久化与灰度迁移 |
| **数值稳定性加固 + 熵计算修正**（修复 F3、浮点稳定性、空分支不确定性） | ~0.5 人·天 | 消除 NaN/Inf 传播，使不确定性度量符合设计契约 |

> **最高杠杆单项修复：完整快照 + 事务式 restore**  
> 一次性封杀 F1 及相关的快照缺失问题，成本仅 3 人·天，收益覆盖核心回滚一致性，并为后续所有序列化/回滚功能打下基础。

### 🟡 P1 ‒ “快”（性能与可扩展）—— 下一里程碑
| 任务 | 投入估算 | 收益 |
|------|----------|------|
| **分词器抽象化**（注入 `tokenizer` 接口，默认保留 `split(" ")`，示例接入 `unicode-segmentation`） | ~1 人·天 | 解锁 CJK 语料，避免灾难性粗粒度 |
| **ANN 索引骨架**（引入纯 MoonBit 的球树/倒排索引，`remember`/`recall` 走索引） | ~5-8 人·天 | 将 O(n²) 降为 O(log n)，支撑 100k+ 节点 |

### 🟢 P2 ‒ “美”（可维护性与健壮性）—— 持续改进
| 任务 | 投入估算 | 收益 |
|------|----------|------|
| 提取公用 JSON 辅助函数（修复 F6） | 0.3 人·天 | 消除代码重复 |
| 统一 `clock` 推进点（修复 F5） | 0.1 人·天 | 消除超参量纲偏移 |
| 并发安全封装（RwLock 或 Actor） | 3-5 人·天 | 为多线程/Serverless 就绪 |
| Property-based 测试、审计日志 | 2-3 人·天 | 提升工程信心，防止回归 |

---

### 一句话建议
> **先把“能回滚、能持久、能算对”做稳（P0），再谈“跑得快”（P1）和“长得美”（P2）—— 当前代码库核心不变量已破损，任何扩展性投入都是在沙堆上盖楼。**