# 译脉·先知 2.0（MoonBit）功能拓展路线图：快 · 准 · 美

> 本文基于 MoA 多模型综合分析（deepseek 聚合，nemotron 节点超时但聚合完整）与 ardot「设计转代码」视角，结合本仓库实际代码（engine.mbt、D1–D8 八大模块、wasm-gc 零依赖实现、Hit@3=0.825、91/91 测试）整理而成。目标：翻得对，还要快、准、美。

## 一、现状基线（已知事实，非估算）

| 维度 | 当前状态 |
|---|---|
| 架构 | 纯 `core/json` + `core/math`，零第三方依赖，逻辑时钟保证确定性可复现 |
| 预测能力 | 一阶马尔可夫转移（D3）+ 赫布学习（D1）+ 角色级抽象（D8，取文本前四字） |
| 实测指标 | Hit@3=0.825；相对频率先验 1.65×、相对随机基线 3.57×；留出集泛化 8/8=100% |
| 验证 | 91/91 测试（含 B1–B4 基线/留出/鲁棒/召回、R1–R22 路线图与 #22 TM/TB 边界用例、E1–E13 扩展能力 #2–#7 回归） |
| 已知瓶颈 | `spread_activation` 全图遍历；`consolidate` 全量扫描；角色键仅「前四字」单粒度；预测为纯记忆式，无人类反馈闭环 |

## 二、处理速度（快）—— 推至更大语料而不崩

当前 30 领域 × 5 轮仅 222–224 条边，图规模尚小，速度不是急迫问题；但**架构选择必须现在定**，否则语料增长一千倍后重构成本极高。

### 短期（高 ROI，低风险）
- **角色倒排索引 + Top-K 激活剪枝**：建立 `Role→Vec<NodeId>` 倒排，`spread_activation` 仅遍历与输入角色相关节点，每步只保留 Softmax 后 Top-K（K=8）。预计 Hit@3 衰减 <0.02，却把 O(N) 全图扫描降为 O(相关节点)。这是**性价比最高**的一项。
- **静态/动态图分离（CSR 雏形）**：静态邻接用连续 `Array[UInt]`（offset+neighbor+weight），增量部分保留尾部 HashMap，批量 flush 合并。消除热路径边界检查与每边分配。

### 中期
- **增量固化（WAL / 分代压缩）**：用写前日志只记录增减，积累到阈值或空闲时批量 compact；高频区高频合并、低频区延迟合并。把 `consolidate` 的全量扫描峰值延迟降低 5–10 倍。
- **预测缓存**：对高频 `(source, role)` 组合缓存预测结果，LRU 淘汰，记忆更新时局部失效。

### 长期
- **定点 / SIMD 重写**：权值改 Q16.16 定点乘法 + 移位，或 `f32` + `core/simd` 的 `f32x4` 并行做衰减与归一化。仅在 profiling 显示浮点为瓶颈时再做。
- **外存层次**：冷数据经宿主 JS 桥接落 IndexedDB，热数据驻 wasm 内存，LRU 页表换入换出。

> 我的判断：速度工作的**主线是「架构先行、按需优化」**——先落地角色倒排索引与静态/动态图分离（低风险、解锁规模），SIMD/定点等留到 profiling 实证后再上，避免 premature optimization。

## 三、学习能力（准）—— 从记忆走向推理

项目护城河是 D8 角色抽象 + 白盒可解释。应在此基础上做加法。

### 短期（清晰、低风险）
- **二阶衰减马尔可夫**：`P(w3|w1,w2)=λ·C(w1,w2,w3)/C(w1,w2)+(1-λ)·P(w3|w2)`，仅存 count>2 的转移，存储增幅 <1.5×。直接提升序列预测准度。
- **多粒度角色泛化**：D8「前四字」扩展为「前二 / 前四 / 前后各二」三键，检索加权合并；冷启动节点挂最近原型继承出边。强化本已擅长的零样本泛化。
- **自适应学习率 + 弹性遗忘**：滑动窗口监控错误率，连续失败临时降 HEBB_LR；边增「最后命中时间戳」，离线衰减按真实时间差折扣。显式解决「遗忘与巩固权衡」（当前 `consolidate` 仅有衰减、无真正遗忘）。

### 中期
- **注意力式边权（仅扩散时计算，不持久化）**：`edge_weight=base×exp(α·sim+β·role_sim)`，轻量算子，不改图存储。
- **领域偏置向量（ΔW_domain）**：每领域维护 `Array[Float]`（维度=角色原型数），推理时 `logits+=ΔW_domain[role]`。轻量 LoRA 式跨域适应。
- **在线对比学习**：用户采纳/拒绝构成 `(ctx, pos/neg)`，正样本 +LR、负样本 -LR/2，与 D7 置信度联动。补上「人类反馈闭环」这一当前最大缺口。

### 长期
- **神经符号混合蒸馏**：定期导出高频 n-gram，外部训练四层 Transformer，蒸馏为 `transition_bias` 只读表注入 PMN。需独立训练流程，工程复杂，仅当准度触顶时启用。
- **跨域联邦协作**：多译员仅上传增量计数，服务端 FedAvg 聚合下发全局原型表，隐私无原文。

> 我的判断：单位投入产出最高的「准」来自两条——（a）**更丰富的角色 schema**（多粒度 + 领域偏置），（b）**来自用户反馈的在线对比学习**。二阶马尔可夫是低风险明确增量。神经符号蒸馏是天花板手段，非必需。

## 四、美 —— 可消费的「快·准·美」闭环（ardot 视角）

「美」不是花哨 UI，而是：术语一致、风格统一、双语对齐，并把 D7 白盒变成译员信任的「为什么」。ardot 的价值在于把这部分定义为**可设计、可转代码的译员工作台**。

### 短期
- **术语节点（TermNode）**：新增节点类型，命中时注入高初始激活（+5.0），输出标注「术语命中」徽标。直接提升一致性与信任。
- **可解释推理卡片**：D7 输出结构化 `Explain{activation_path, prediction_path, value_breakdown}`；CLI `--explain` 显示缩进树，前端渲染为「证据链」。这是「美」的核心交付——**以透明建立信任**。

### 中期（ardot 设计转代码旗舰）
- **职业译员工作台**：Monaco 式双语 TM 面板 + 实时预测流 + 可解释侧栏 + 术语高亮；Web 与 CLI 共享同一 wasm 模块（`core/http` 起本地服务）。界面规格：左源文 / 右译文，中部预测流卡片，右侧「证据链」可解释面板，术语命中处高亮。
- **双语对齐热力图**：基于 Myers diff 的字符级对齐，前端高亮对应片段并着色编辑距离。
- **主动学习推荐**：按 `uncertainty=1-confidence` 与 `diversity=min_sim(pool)` 推送 Top-K 待标注句。

### 长期
- **可视化记忆图谱**：引擎输出节点边 JSON，D3.js 力导向图，译员可浏览「哪些记忆活跃」「矛盾边」。
- **风格一致报告**：统计记忆库平均句长、正式度、术语变体频率，新译文偏离分布时建议或自动回退。

## 五、我拍板的落地优先级

| 阶段 | 核心聚焦 | 关键任务 | 对三目标贡献 |
|---|---|---|---|
| **短期（0–6 周）** | 极速白盒 | 角色倒排索引 + Top-K 剪枝；二阶马尔可夫；多粒度角色；自适应遗忘；TermNode；可解释卡片 | 快⚡⚡⚡ 准🎯🎯🎯 美✨✨ |
| **中期（6–16 周）** | 译员工作台 | 增量固化；领域偏置 + 在线对比学习；双语对齐；Web/CLI 工作台（ardot 转代码） | 快⚡⚡ 准🎯🎯🎯 美✨✨✨ |
| **长期（16–32 周）** | 联邦预知 | 外存层次；神经符号蒸馏；联邦协作；可视化图谱；风格报告 | 快⚡ 准🎯🎯🎯🎯 美✨✨✨✨ |

## 六、贯穿始终的取舍原则

- **零依赖铁律**：核心算法手写于 `core/math`、`core/simd`、`core/buffer`；外部模型仅作离线蒸馏产物。
- **确定性优先**：随机性经显式 `seed` 控制，逻辑时钟单调递增——这是项目差异化资产，必须保护。
- **增量 ∥ 批量**：拒绝全量重建，在线学习与增量固化为默认模式。
- **可解释即接口契约**：`Explain` 结构体是对外契约，前端 / CLI 只消费 JSON，不耦合内部图结构。

## 七、下一步建议

1. 立即开工短期第一项（角色倒排索引 + Top-K 剪枝）：低风险、解锁规模、几乎零准度损失。
2. 同步补「可解释推理卡片」：纯接口层，无算法风险，却直接提升「美」与信任。
3. 「美」的旗舰——职业译员工作台——可用 ardot 设计转代码流程产出高保真原型（HTML/CSS/JS），作为 GitHub 主页演示与产品化入口。

## 八、引擎侧实现状态追踪（2026-07-29 更新）

> 状态来自 `engine.mbt` / `util.mbt` 实际代码 + `moon test`（**91/91 通过**，Hit@3=0.8246，确定性逐字节一致、JSON 往返一致、重启后预测一致均 PASS；新增 R15 强化「重启后 predict 逐字节一致」硬契约；#22 TM/TB 边界用例 R16–R22 全部 PASS；#2–#7 扩展能力 E1–E13 全部 PASS）。

| 路线图条目 | 引擎侧实现 | 状态 |
|---|---|---|
| 角色倒排索引 + Top-K 剪枝 + 预测缓存 LRU | `role_members` / predict Top-K / `pred_cache` | ✅ 已实现（引擎） |
| 二阶马尔可夫 | `trans2`（`prev2::prev`→{w3}） | ✅ 已实现（引擎） |
| 多粒度角色 | `roles_of`（前二/前四/前后各二） | ✅ 已实现（引擎） |
| 自适应学习率 + 弹性遗忘 | `hebb_lr` 自适应 / `consolidate` 时差折扣衰减 | ✅ 已实现（引擎） |
| 领域偏置 ΔW（LoRA 式） | `domain_bias` + `set_domain_bias` + `inject_distillation` | ✅ 已实现（引擎） |
| 在线对比学习 | `cl_step(anchor,pos,neg)` | ✅ 已实现（引擎） |
| 注意力边权 | `attn_alpha/attn_beta` + `set_attention` + recall 门控 | ✅ 已实现（引擎） |
| TermNode + 可解释卡片 | `mark_term` / `explain_card{activation_path,prediction_path,value_breakdown}` | ✅ 已实现（引擎） |
| 增量固化 WAL | `wal_replay/export/compact/clear/len` | ✅ 已实现（引擎） |
| 双语对齐（Myers/LCS） | `align_diff` 字符级编辑脚本 | ✅ 已实现（算法层） |
| 主动学习候选 | `active_learning_candidates(k)` | ✅ 已实现（引擎） |
| 联邦增量导出/导入 | `fed_export/fed_import`（计数聚合） | ✅ 已实现（引擎·增量层） |
| 神经符号蒸馏 | `inject_distillation`（只读 ΔW 表注入） | ✅ 已实现（注入侧） |
| #22 翻译记忆 TM / 术语库 TB（大赛扩展能力 #1） | `add_tm` / `fuzzy_match`（0.7·token-cosine + 0.3·char-ratio）/ `concordance` / `load_tbx`（TBX ISO 30042 语言感知）/ `enforce_terms`·`check_terms`（词边界 `term_hit`） | ✅ 已实现（引擎 · #22） |
| #2 质量评估 QE + MQM | `qe_score` / `mqm_tags` / `qe_auto` | ✅ 已实现（引擎 · #2） |
| #3 格式保真往返 | `check_format_fidelity` / `protect_tags` | ✅ 已实现（引擎 · #3） |
| #4 多模态/截图翻译 | `ocr_image`（外部桩）/ `align_regions` | ✅ 已实现（引擎 · #4；OCR 为外部边界） |
| #5 本地化 CI / 批处理 | `batch_apply` | ✅ 已实现（引擎 · #5） |
| #6 TMS 互通 | `parse_tmx` / `parse_xliff` / `export_tmx` | ✅ 已实现（引擎 · #6） |
| #7 可观测 / 漂移监控 | `metrics` / `drift_report` | ✅ 已实现（引擎 · #7） |
| 外存层次 / CSR / WAL 持久化 | — | ⬜ 需外部系统（本版 wasm-gc 内存态） |
| 职业译员工作台（ardot 转代码） | — | ⬜ 需 ardot 衔接（Web/CLI 前端） |
| 双语对齐热力图（前端渲染） | — | ⬜ 需 ardot 衔接（算法已就绪） |
| 联邦协调方（FedAvg 聚合服务） | — | ⬜ 需外部服务（引擎仅出增量） |
| 神经符号蒸馏的「外部训练流程」 | — | ⬜ 需独立训练管线（引擎仅消费蒸馏表） |
| 可视化记忆图谱 | — | ⬜ 需前端（数据接口已可消费） |

**结论**：路线图「快·准·美」的**算法/引擎内核**已全量落地并通过契约测试；**#22 翻译记忆 TM/术语库 TB（大赛扩展能力 #1）亦已在引擎侧实现**（91/91 测试含 R16–R22 边界用例：语言感知 TBX、词边界防 `log`⊂`logical` 误命中、三语种锁定），**#2–#7 六项扩展能力亦全部在引擎侧实现并通过 E1–E13 回归**。剩余 ⬜ 条目均为**前端工作台（ardot 衔接）或外部服务（联邦协调方、外存、蒸馏训练管线）**，属「需外部系统」而非「未实现」。三大约束契约在全部 Phase 后依然保持。更大的 **#1–#7 扩展能力追踪见 README「Roadmap & Extension Status」**。

